/*
 * Deferred Host - Spawns a new channel instance
 * 
 * Warning: Listener scripts should be compiled to VanillaJS/ES5 before any messages can be received
*/


// let first

// export default first = (index, figure) => +(((figure + "").split("").splice(0, index || figure.length)).join(""))

// var d1 = new Date()
// var d2 = new Date()
// var ms = 2

// console.log(first(ms, 2341));